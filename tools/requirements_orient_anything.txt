# Optional deps for Orient Anything planning hints.
# The main progressive-pose environment already installs most of these.
# Keep the existing torch/torchvision stack pinned; do not install Orient-Anything's
# torch==2.2.1 requirement over the MotionEdit environment.
transformers>=4.57.0,<5.0
huggingface-hub>=0.34.0
numpy==1.26.4
pillow>=10.2.0

# Only needed for the standalone Orient-Anything Gradio/background-removal/demo utilities:
# matplotlib
# gradio==5.9.0
# onnxruntime
# rembg
