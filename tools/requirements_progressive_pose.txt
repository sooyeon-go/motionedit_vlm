# Pinned deps for inference/progressive_pose_edit.py
# Qwen3-VL needs transformers>=4.57; transformers 5.x breaks peft HybridCache import.
# Pin torch stack separately; do not let pip float torch/torchvision.
# torchao 0.17+ requires torch>=2.11; use 0.16.x with torch 2.6.
transformers>=4.57.0,<5.0
peft>=0.18.0
diffusers==0.36.0
torchao>=0.16.0,<0.17.0
huggingface-hub>=0.34.0
qwen-vl-utils
accelerate
safetensors
packaging
# torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0  (install via fix_dependencies.sh)
